import { Text } from "react-native";

/**
 * Screen Component where the user can view the safety info
 * @returns a created SafetyInfoScreen Component
 */
export default function SafetyInfoScreen() {
  return <Text> SafetyInfoScreen </Text>;
}
// End of File
