import React from 'react';
import './loginRegisterInputComponent.css'; // Import the CSS for styling

// Define the component as a functional component
const loginRegisterInputComponent: React.FC = () => {
    return (
        <button className="student-email-btn">
            Student Email
        </button>
    );
};

export default loginRegisterInputComponent;